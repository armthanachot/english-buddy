# Drizzle CLI

- `npx drizzle-kit generate` to generate migration file to the following path in [drizzle.config.ts](./drizzle.config.ts)
- `npx drizzle-kit migrate` apply new migration file to DB
- `npx drizzle-kit push` apply to DB without SQL migration file ⚠️ (not friendly on production)

